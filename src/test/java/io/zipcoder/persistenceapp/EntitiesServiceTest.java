package io.zipcoder.persistenceapp;

import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.*;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class EntitiesServiceTest {

//    @Test
//    public void create() {
//
//        Employee employee = new Employee();
//        EntityRepo mockRepo = mock(EntityRepo.class);
//
//        when(mockRepo.save(employee)).thenReturn(new Employee(1));
//        EntitiesService entitiesService=new EntitiesService(mockRepo);
//        Employee actual = entitiesService.create(employee);
//        Assert.assertEquals(1, actual.getID());
//    }
}